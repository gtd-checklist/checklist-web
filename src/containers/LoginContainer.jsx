import React, { PureComponent } from 'react';

// eslint-disable-next-line react/prefer-stateless-function
export default class LoginContainer extends PureComponent {
  render() {
    return (
      <h1>Writing from LoginContainer</h1>
    );
  }
}
