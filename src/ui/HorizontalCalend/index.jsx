import React from 'react';
// import PropTypes from 'prop-types';

import Grid from '@material-ui/core/Grid';

const HorizontalCalend = () => {
  const date = {};
  return (
    <Grid container>
      календарь
    </Grid>
  );
};

HorizontalCalend.propTypes = {
};

HorizontalCalend.defaultProps = {
};

export { HorizontalCalend };
